const Express = require('Express');
const App = Express();
const port = 80;

const CORS = require('cors');
App.use(CORS());


App.listen(port, () => {

App.get("movies/title/:title", (req,res) => {




})


});